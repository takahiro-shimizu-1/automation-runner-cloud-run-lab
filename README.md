# automation-runner-cloud-run-lab

`automation-hub` の deploy-enabled profile を Cloud Run preset で検証する sandbox repo。

- app 本体は `app/`
- repo 固有設定は `config/miyabi-repository.json`
- product deploy command は `scripts/product/*.sh`
- 共通 automation runner の cloud-run preset dry-run 疎通確認に使う
